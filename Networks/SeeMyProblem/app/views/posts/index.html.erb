<p id="notice"><%= notice %></p>

<h1>Posts</h1>
<select class="main_filter">
  <option value="All" selected="selected">All</option>
  <option value="1st">1st</option>
  <option value="2nd">2nd</option>
  <option value="3rd">3rd</option>
  <option value="Personal">Personal</option>
  <option value="Other">Other</option>
</select>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Content</th>
      <th>Type</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.content %></td>
        <td><%= post.post_type %></td>
        <td><%= link_to 'Show', post %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<h1>New post form with Ajax</h1>
<%= form_for(@post, remote: true) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% @post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :title %>
      <%= f.text_field :title, id: :post_title %>
    </div>

    <div class="field">
      <%= f.label :content %>
      <%= f.text_area :content, id: :post_content %>
    </div>

    <div class="field">
      <%= f.label :post_type %>
      <%= f.select :post_type, options_for_select([["Select One", ""], "1st", "2nd", "3rd", "Personal", "Other"]) %>

    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>



<h1>New post form without ajax</h1>
<%= link_to 'New Post', new_post_path %>
